@inherits HypinessComponent

<div class="px-4 py-5 md:!hyp-p-6 @(IsGrayed? "hyp-bg-gray-200" : "hyp-bg-gray-50") rounded-lg">
    @ChildContent
</div>
@code {
    [Parameter, EditorRequired]
    public RenderFragment ChildContent { get; set; } = null!;

    [Parameter]
    public bool IsGrayed { get; set; }
}
